version: "3.9"

services:
  api:
    build: .
    env_file:
      - .env
    environment:
      - TOGETHER_API_KEY=${TOGETHER_API_KEY:-}
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    command: uvicorn api.api_server:app --host 0.0.0.0 --port 8000
